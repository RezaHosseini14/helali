export type ListImageType = {
  title: string;
  url: string;
};

export type ListAudioType = {
  title: string;
  audioType: string;
  audioDesc: string;
  url: string;
};

export type AudioDetails = {
  audioType: string;
  audioDesc: string;
};
